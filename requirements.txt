# Ubuntu 22.04 + Python 3.10 优化版本
# 核心机器学习和深度学习包
torch==2.5.1  # 降级到更稳定的版本，兼容Ubuntu 22.04
transformers==4.46.3  # 降级到更稳定的版本
tokenizers==0.20.3  # 与transformers版本匹配
safetensors==0.4.5  # 降级到更稳定的版本
huggingface-hub==0.26.2  # 降级到更稳定的版本

# Streamlit和Web界面相关
streamlit==1.39.0  # 降级到更稳定的版本
altair==5.2.0  # 降级到更稳定的版本
pydeck==0.8.0  # 降级到更稳定的版本

# 数据处理和科学计算
numpy==1.24.4  # 使用与Python 3.10兼容的稳定版本
pandas==2.0.3  # 降级到更稳定的版本
pyarrow==14.0.2  # 降级到更稳定的版本

# 图像处理
pillow==10.4.0  # 降级到更稳定的版本

# 网络和HTTP
requests==2.31.0  # 升级到更安全的版本
urllib3==2.0.7  # 降级到兼容版本
certifi==2024.8.30  # 保持较新但稳定的版本

# 序列化和配置
protobuf==4.25.5  # 降级到更稳定的版本，避免兼容性问题
PyYAML==6.0.2
toml==0.10.2

# 系统和工具包
packaging==24.1
filelock==3.16.1
fsspec==2024.9.0
tqdm==4.66.5
click==8.1.7
colorama==0.4.6

# Git相关
GitPython==3.1.43
gitdb==4.0.11
smmap==5.0.1

# JSON和数据验证
jsonschema==4.23.0
jsonschema-specifications==2024.10.1
referencing==0.35.1
rpds-py==0.20.0

# 文本和标记处理
markdown-it-py==3.0.0
mdurl==0.1.2
Pygments==2.18.0
rich==13.8.1

# 模板和标记
Jinja2==3.1.4
MarkupSafe==2.1.5

# 时间和日期
python-dateutil==2.9.0.post0
pytz==2024.2
tzdata==2024.2

# 数学和科学计算
sympy==1.13.3
mpmath==1.3.0
networkx==3.2.1

# 其他工具包
attrs==24.2.0
blinker==1.8.2
cachetools==5.5.0
charset-normalizer==3.4.0
idna==3.10
narwhals==1.13.5
regex==2024.9.11
six==1.16.0
tenacity==9.0.0
tornado==6.4.1
typing_extensions==4.12.2